{% extends 'back/layout_admin.html.twig' %}
{% block contant %}
    <div id="page-content">
        <div class="panel">
            <h1 style="padding:0 30px;">Edite Immobilaire</h1>

            <hr>
            <div class="row padding90">

                <h1>Immobiliair edit</h1>

                {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <input type="submit" value="Edit" />
                {{ form_end(edit_form) }}

                <ul>
                    <li>
                        {#<a href="{{ path('immobiliair_index') }}">Back to the list</a>#}
                    </li>
                    <li>
                        {#{{ form_start(delete_form) }}#}
                        {#<input type="submit" value="Delete">#}
                        {#{{ form_end(delete_form) }}#}
                    </li>
                </ul>

            </div>
        </div>
    </div>
    <!--===================================================-->
    <!--End page content-->
{% endblock %}
